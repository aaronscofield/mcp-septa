[project]
name = "mcp-septa"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.9.2",
    "pydantic>=2.11.5",
    "httpx>=0.27.0",
]

[tool.uv]
package = true

[project.scripts]
mcp-septa = "mcp_septa.mcp.server:main"

[dependency-groups]
dev = [
    "black>=25.1.0",
    "pylint>=3.3.7",
]
